#!/usr/bin/env bash

CYAN=$'\033[1;36m'
NO_COLOR=$'\033[0m'
LABEL="db-create"
printf "%s==== %s%s\n" "${CYAN}" "${LABEL}" "${NO_COLOR}"

# NO_DB_CONNECTION_URL=$(sed 's/\/cruddur//g' <<<"$CONNECTTION_URL")

PGPASSWORD="$DB_PASSWORD" psql -h localhost -Upostgres -c "CREATE DATABASE cruddur;"
