#!/usr/bin/bash

CYAN=$'\033[1;36m'
NO_COLOR=$'\033[0m'
LABEL="db-connect"
printf "%s==== %s%s\n" "${CYAN}" "${LABEL}" "${NO_COLOR}"

if [ "$1" == "prod" ]; then
    echo "Connecting to production..."
    URL=$PROD_CONNECTION_URL
else
    URL=$CONNECTION_URL
fi

psql "$URL"